{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@types/prettier": "^2.7.1",
    "@types/sass": "^1.43.1",
    "nuxt": "3.0.0",
    "prettier": "^2.8.1",
    "sass": "^1.57.0"
  },
  "dependencies": {
    "normalize.css": "^8.0.1"
  }
}
